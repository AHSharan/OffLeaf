# Leaf masks for PlantDoc (field imagery; expect a higher HSV fallback rate).
#   python masks/leaf_masks.py --config configs/leaf_masks_plantdoc.yaml

image_root: data/raw/plantdoc
mask_root: data/masks/leaf
checkpoint: weights/sam_vit_b_01ec64.pth
model_type: vit_b
score_threshold: 0.85
mobile_sam: false
seed: 0
