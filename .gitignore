# Virtual environment
.venv/

# Datasets (large, re-downloadable)
data/raw/
data/masks/
data/counterfactual/

# Experiment outputs and model weights
runs/
weights/
*.pt
*.pth

# Derived splits. Regenerate deterministically:
#   python data/splits.py --config configs/splits.yaml
# If you need to freeze exact membership across machines, publish them with the
# benchmark (release/push_benchmark.py) rather than committing them here.
data/splits/*.csv

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Credentials
.kaggle/
